export enum TitleErrorMessages {
  required = 'Title is required',
  minlength = 'Title must be at least 3 characters long',
  maxlength = 'Title must be 20 characters maximum',
}

export enum DescriptionErrorMessages {
  required = 'Title is required',
}
